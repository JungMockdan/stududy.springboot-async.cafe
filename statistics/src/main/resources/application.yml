#logging
logging:
  level:
    com.jmd.cafe.statistics: debug

# server
server:
  error:
    whitelabel:
      enabled: false
  port: 8082

#fiegn
feign:
  server:
    order:
      name: "order-server"
      url: "http://localhost:8080/"

